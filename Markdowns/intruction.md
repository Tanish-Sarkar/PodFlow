Check the code base and study `@V1.5.md` to understand what you need to do.

# Tasks:
1. remove the login-logout system entirely,
2. make a landing page - that explain the app , and its use cases. and it must be atleast 2 full scroll long. From there, there will be a button "use agent" to get to the main flow.
3. inside the main flow, remove the auth check as well, and just let people use the app.
4. but i need the email sending and AI capabilities so keep them. 
5. the old "google-generativeai" python libaray is deprecated so i have updated the  `migration.md` for you to implement the changes in `src` directory in root.


**The UI and design guide refer `@design.md` and `@PodFlow_Brand_Adaptation.md`**